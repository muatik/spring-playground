spring.datasource.url=jdbc:mysql://localhost/example?useSSL=false
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.show-sql=true
flyway.baseline-on-migrate=true


# 'create" creates a new schema from scratch, present data will be gone.
# spring.jpa.hibernate.ddl-auto=create

# 'validate' means no-auto generation is allowed, only checking
# existing schema whether it has tables and table columns which matches
# matches existing jpa entities.
# Tables might have extra columns, however, not having necessary column will
# cause violation, and validation will fail.
# spring.jpa.hibernate.ddl-auto=validate

# like 'create', this will create database schema automatically.
# In addition, while app is being stopped, the auto-generated
# schema is dropped. This could be ideal for testing since it
# ensures same database states for each application context.
# spring.jpa.hibernate.ddl-auto=create-drop


# updates existing db schema, if not exists, creates a new one.
# this does not touch existing columns, just create new columns. so
# it prevents data lost
# spring.jpa.hibernate.ddl-auto=update



